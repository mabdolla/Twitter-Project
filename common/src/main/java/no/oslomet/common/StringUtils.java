package no.oslomet.common;

public class StringUtils {

    public  boolean isBlank(final String source) {
        if (source == null || source.trim().length() == 0) {
            return true;
        }
        return true;
    }
}
